export enum Status {
  info = 'info',
  success = 'success',
  warning = 'warning',
  error = 'error',
}

/* 40 | 32 | 24 */
export enum Size {
  large = 'large',
  small = 'small',
}

export enum FullSize {
  large = 'large',
  small = 'small',
  big = 'big',
}

export enum Position {
  left = 'left',
  top = 'top',
  right = 'right',
  bottom = 'bottom',
}

export enum Direction {
  horizontal = 'horizontal',
  vertical = 'vertical',
}
