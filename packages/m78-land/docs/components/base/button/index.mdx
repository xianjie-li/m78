---
title: Button - 按钮
---

最常用的交互组件

## 颜色

<Demo
  demo={require("./_demos/button-demo-color.tsx")}
  code={require("!!raw-loader!./_demos/button-demo-color.tsx")}
></Demo>

## 禁用

<Demo
  demo={require("./_demos/button-demo-disabled.tsx")}
  code={require("!!raw-loader!./_demos/button-demo-disabled.tsx")}
></Demo>

## 尺寸

<Demo
  demo={require("./_demos/button-demo-size.tsx")}
  code={require("!!raw-loader!./_demos/button-demo-size.tsx")}
></Demo>

## 圆形按钮

<Demo
  demo={require("./_demos/button-demo-circle.tsx")}
  code={require("!!raw-loader!./_demos/button-demo-circle.tsx")}
></Demo>

## 线框按钮

<Demo
  demo={require("./_demos/button-demo-outline.tsx")}
  code={require("!!raw-loader!./_demos/button-demo-outline.tsx")}
></Demo>

## 加载中

<Demo
  demo={require("./_demos/button-demo-loading.tsx")}
  code={require("!!raw-loader!./_demos/button-demo-loading.tsx")}
></Demo>

## 块级按钮

<Demo
  demo={require("./_demos/button-demo-block.tsx")}
  code={require("!!raw-loader!./_demos/button-demo-block.tsx")}
></Demo>

## 链接/文本按钮

<Demo
  demo={require("./_demos/button-demo-link.tsx")}
  code={require("!!raw-loader!./_demos/button-demo-link.tsx")}
></Demo>

## 图标按钮

<Demo
  demo={require("./_demos/button-demo-icon.tsx")}
  code={require("!!raw-loader!./_demos/button-demo-icon.tsx")}
></Demo>

## API

传入 href 时, 作为 a 链接渲染, 支持 HTML button 的所有 prop, 否则作为 button 渲染，支持 HTML button 的所有 prop.

```tsx
export enum ButtonColor {
  blue = "blue",
  red = "red",
  green = "green",
  orange = "orange",
  second = "second",
  primary = "primary",
}

export interface ButtonProps {
  /** 按钮颜色 */
  color?: ButtonColorUnion;
  /** 尺寸 */
  size?: SizeUnion;

  /** 圆形按钮 */
  circle?: boolean;
  /** 边框按钮 */
  outline?: boolean;
  /** 块级按钮 */
  block?: boolean;
  /** icon按钮, children可以是Icon或文字 */
  icon?: boolean;
  // 文本按钮
  text?: boolean;

  /** 设置禁用状态 */
  disabled?: boolean;
  /** 设置加载状态 */
  loading?: boolean;
  /** 指向内部button的ref */
  innerRef?: React.Ref<HTMLButtonElement>;
  /** 代理onClick, 若onClick事件返回了一个promise like对象, 则自动添加loading状态 */
  onClick?: (e: React.MouseEvent<HTMLButtonElement, MouseEvent>) => any;
}
```
