{
  "name": "@m78/icons",
  "version": "1.0.0",
  "description": "material icons to react component",
  "main": "./index.js",
  "scripts": {
    "test": "jest",
    "lint:prettier": "prettier ./src ./test --write --no-error-on-unmatched-pattern",
    "lint:script": "eslint ./src ./test --ext .js,.jsx,.ts,.tsx,.vue --fix",
    "lint": "npm run lint:script && npm run lint:prettier",
    "build": "node ./script/transform.js",
    "pub": "pnpm build && pnpm publish --access public --registry https://registry.npmjs.org --no-git-checks",
    "example-hello": "mbt example hello"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "files": [
    "**"
  ],
  "type": "module",
  "publishConfig": {
    "directory": "dist",
    "linkDirectory": true
  },
  "devDependencies": {
    "@material-design-icons/svg": "^0.14.2",
    "@svgr/core": "^6.5.1",
    "glob": "^8.0.3",
    "lodash": "^4.17.21"
  },
  "dependencies": {
    "clsx": "^1.2.1",
    "rollup": "^3.4.0"
  }
}
